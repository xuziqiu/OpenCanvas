# OpenCanvas 1.0.6 本地交付记录

验证日期：2026-08-21

## 交付物

- 安装程序：`release/OpenCanvas Setup 1.0.6.exe`
  - 大小：106,260,825 bytes
  - SHA-256：`E04D19B7FF8C0D533240850CDA6B1410453DB819F9E9875BB1D77AB5036E10CD`
- 免安装程序：`release/win-unpacked/OpenCanvas.exe`
  - 大小：225,619,456 bytes
  - SHA-256：`DD13324C60E53A1DACA672C1EBDA158F6BE9147ACD5721BFDAFBA1E9482A12CF`

构建产物保留在本地并由 `.gitignore` 排除。公开发布时，安装程序和校验文件应作为 GitHub Release 附件上传，不应提交进 Git 历史。

上述二进制生成于本次源码卫生整理之前，校验值只对应当前保留的本地文件。公开发布前应从清理后的首次提交重新打包，并据新文件更新大小与 SHA-256，不能把旧校验值当作新构建的证明。

## 源码验证

本次本地 Git 清理后执行：

- `npm run build`
- 72 个测试文件、339 项测试全部通过
- TypeScript 类型检查通过
- Knip 源码、直接依赖与未声明依赖卫生检查通过
- 浅色/深色主题对比度预算通过
- 高频紧凑控件交互尺寸预算通过
- Vite 生产构建通过
- 主入口 443.5 KiB，构建体积预算通过
- `npm audit --omit=dev --audit-level=high`：0 个已知漏洞
- 隔离 Electron 全链路回归通过：5000 节点白板首屏 134 ms、挂载 20 个 DOM 节点；文件、编辑器、Section、嵌套白板、连线、恢复和真实磁盘往返均通过

Electron 回归首次运行曾在清空 5000 项图层搜索后遇到一次 3 秒等待超时；立即隔离复跑完整通过。该等待已放宽到 5 秒，白板首屏的独立性能预算保持不变，用于避免把 Windows 调度抖动误报为产品回归。

## 发布边界

- 当前安装程序尚未进行 Windows 代码签名。
- 项目自身许可证尚未确定，因此 `package.json` 暂时标记为 `UNLICENSED`；正式公开发布前必须明确许可证。
- Heptabase 解包目录、QA 工作区、本地知识库与构建产物均不属于源码交付物。
- 所有直接依赖均锁定明确版本，避免执行 `npm install` 时因 `latest` 漂移改变已经验证的工具链。
